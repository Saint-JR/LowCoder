import { memo } from 'react';

const TopLayout = memo(() => {
  return (
    <div className="h-[50px] w-full shrink-0 bg-[var(--low-coder-bg-gray)] border-b-[1px] border-solid border-[var(--low-coder-border-gray)]">
      123
    </div>
  );
});

export default TopLayout;
